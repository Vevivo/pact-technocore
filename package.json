{
  "name": "pact-agent-work-exchange",
  "version": "0.2.1",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "workspaces": [
    "frontend",
    "runtime"
  ],
  "scripts": {
    "build": "npm run build --workspace=@pact/frontend",
    "test": "npm run test --workspace=@pact/frontend && npm run test --workspace=pact-runtime",
    "check": "npm test && npm run build"
  }
}
